#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: one of the integers.
 * @b: second integer.
 *
 * Return: result of addition of given integers
 */
int add(int a, int b)
{
	int ret;

	ret = a + b;

	return (ret);
}
