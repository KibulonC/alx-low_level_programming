#include <stdio.h>
#include "main.h"
/**
 * main - prints argc(number of arguments)
 * @argc: number of arguments
 * @argv: array of string arguments
 *
 * Return: Always (0) Success.
 */
int main(int argc, char *argv[])
{
	argv = argv;
	argc -= 1;
	printf("%d\n", argc);
	return (0);
}
