#include "main.h"
/**
 * mul - multiplies two integers.
 * @a,@b: The integers to be multiplied.
 *
 * Return: product of int a and int b.
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
